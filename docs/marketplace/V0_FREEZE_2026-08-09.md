# ShopOps Studio v0 release freeze — 2026-08-09

## Superseded by continuous delivery on `marketplace/stable-prep`

**This freeze is no longer in effect.** The rest of this document describes a point-in-time
snapshot from 2026-08-09 (`v0.1.0-rc.5`). Since then, `marketplace/stable-prep` has moved forward
as a rolling branch under CI gating rather than staying frozen — see
`docs/marketplace/BRANCH_WORKFLOW.md` for the current model. Treat the sections below as
historical evidence of that one release candidate, not the current release state.

## 2026-08-19 status check (verified this session)

Initial premise going into this session was wrong and got corrected mid-session: this repo's
local clone was stale. `git fetch --prune` revealed that dozens of branches (including
`custom/local-friend-deployment`, the branch this repo was checked out to) had already been
deleted from `origin` on 2026-08-15, and `origin/marketplace/stable-prep` had already moved 8
commits past the `v0.1.0-rc.5` freeze point — including all four 2026-08-14 fixes plus further
perf/correctness work and a first CI quality gate (`037ae6c "chore: simplify repository to one
active branch"`, confirmed live on Render as of this session). **`marketplace/stable-prep` is now
the single active branch** (per `docs/marketplace/BRANCH_WORKFLOW.md`, added in that same
consolidation) — there is no more "which branch is really being submitted" question, because
there is only one long-lived branch left.

Verified directly on `origin/marketplace/stable-prep` (clean `npm ci`, from a short-lived branch
`marketplace/rc6-doc-and-fixes` created off it per the repo's own branch workflow):

- Typecheck: passed. Lint: passed.
- Unit tests: 137 P0 tests + 2 staff-CSV tests, all passed.
- Production build: passed. `shopify app build` (POS extension included): passed.
- `npm audit --omit=dev --audit-level=high` — **found 4 high-severity vulnerabilities** (transitive
  via `prisma` → `@prisma/config` → `deepmerge-ts@7.1.5`, GHSA-ggr8-5vv4-36mx). This is the exact
  command `release:verify` runs, so the `quality-gate` CI check would have been **red** on this
  branch. Fixed with a `deepmerge-ts` override to the patched `8.0.1` (build/config tooling only,
  no runtime code path) — `npm audit --omit=dev --audit-level=high` now reports 0 vulnerabilities.

Two small code fixes applied on top (branch `marketplace/rc6-doc-and-fixes`, not yet merged —
opening a PR per `docs/marketplace/BRANCH_WORKFLOW.md`, not merging without explicit approval):
hardcoded `ApiVersion.October25` in `app/shopify.server.ts` (three occurrences) bumped to
`ApiVersion.April26` — the latest version available without a major
`@shopify/shopify-app-react-router` dependency bump — and the unused
`app/lib/permissions/location-access.ts` deleted (zero importers, confirmed by grep, re-verified
directly against `origin/marketplace/stable-prep`'s tree). Also added a controlled CI delivery
system (issue/PR templates, `AGENTS.md`/`CLAUDE.md` agent operating rules, a risk-classifier
script, and a Playwright + axe-core public-page smoke test) — see
`docs/automation/DELIVERY_SYSTEM.md`.

**Also found live on Render, unrelated to code**: the `shopops-maintenance-tick` cron job has
been suspended since 2026-08-08 23:21 EDT (11 days as of this check) — it is not running at all
right now, contradicting the "Render Cron Requirements" section below and in
`MARKETPLACE_ENVIRONMENT_SETUP.md` which describe it as active. Confirm whether this was
intentional before submission.

The `shopops-marketplace-preview` Render web service itself is confirmed genuinely dedicated: it
tracks `marketplace/stable-prep` with auto-deploy on every commit, and the deploy history matches
`origin/marketplace/stable-prep` exactly. No second Render service is needed. It has no custom
domain configured — it is running on the plain `.onrender.com` hostname. Whether that should
remain the permanent production hostname (in which case the "temporary" wording throughout the
marketplace docs and the toml comment should be dropped) or be replaced by a custom domain later
is a decision for the owner, not something inferred here.

## Frozen release

- Release branch: `marketplace/stable-prep`
- Release commit: the commit referenced by the immutable tag below
- Release tag: `v0.1.0-rc.5`
- Render service branch: `marketplace/stable-prep`
- Render deployment: follows `marketplace/stable-prep`; this candidate adds the
  final merchant-facing UI polish and official wordmark to the validated
  release source, evidence, and listing assets

No feature work should enter this release candidate. Any further source change
requires a blocker classification, focused regression coverage, a new release
candidate tag, and a fresh Render smoke test.

## Release evidence

- Typecheck: passed
- Lint: passed
- P0/Marketplace tests: 129 passed, 0 failed
- Production build: passed
- Shopify CLI app build: passed
- Live embedded smoke after App Pricing activation: passed
- Shopify automated common-error check: passed
- Protected Customer Data form: 9/9 questions complete
- Selected Shopify capability: Embedded

## Shopify App Pricing

App Pricing is enabled for new subscriptions.

| Plan           | Handle           |         Price | Trial   | Visibility               |
| -------------- | ---------------- | ------------: | ------- | ------------------------ |
| Solo           | `solo`           | $19 USD/month | 14 days | Public                   |
| Growth         | `growth`         | $49 USD/month | 14 days | Public                   |
| Multi-location | `multi-location` | $99 USD/month | 14 days | Public                   |
| QA Pilot       | `qa-pilot`       |          Free | None    | Private, three QA stores |

All four application plans return through `/app/billing/complete`. Shopify also
maintains its own empty-access `shopify-test` plan for billing-integration tests;
it is private and has no authorized store.

## Prepared listing assets

- `public/marketplace/screenshots/2026-08-final/01-overview-kpis-1600x900.png`
- `public/marketplace/screenshots/2026-08-final/05-location-comparison-1600x900.png`
- `public/marketplace/screenshots/2026-08-final/06-product-costs-1600x900.png`
- `public/marketplace/screenshots/2026-08-final/09-location-access-1600x900.png`
- `public/marketplace/screenshots/2026-08-final/11-data-sync-1600x900.png`
- Six additional `1600x900` product views in the same folder.

The images are cropped to the ShopOps surface, contain no browser chrome or
visible personal identity, and use the fixed synthetic reporting range. Their
alt text is saved in the English listing draft.

## Remaining submission gates

1. Upload the selected final screenshots to the English listing.
2. Record, upload, and paste the required 3-8 minute reviewer screencast URL.
3. Keep Lifecycle B uninstalled until the scheduled real `shop/redact`
   verification on 2026-08-11 at 02:05 UTC.
4. Complete Shopify's AI self-review and the owner's final App Store
   Requirements attestation.
5. Owner performs the final `Submit for review` action.

The authenticated embedded LCP trace remains a performance/Built for Shopify
follow-up. The public production baseline on 2026-08-09 measured LCP 365 ms,
TTFB 138 ms, and CLS 0.00; Shopify's field dashboard still reports embedded LCP
p75 around 3.55 s.

## Public website and final capture refresh

- Rebuilt the public Render landing page around the frozen product: profit,
  location comparison, costs, people/access, sync readiness, and the current
  public Shopify App Pricing plans.
- Replaced the obsolete single `$59.99/month` website offer with Solo at `$19`,
  Growth at `$49`, and Multi-location at `$99`, each with a 14-day trial.
- Added the final normalized capture set under
  `public/marketplace/screenshots/2026-08-final/`.
- All 11 final captures are exactly `1600x900`, preserve their original content,
  and use demo data with personal identities obscured where applicable.
- Replaced the temporary public-page app icon with the approved ShopOps Studio
  wordmark.
- Clarified returns versus cash refunds, simplified repeated KPI comparison
  context, improved vendor-share contrast, and made merchant reporting and cost
  tables sortable without removing existing drill-down interactions.
- Restored the primary blue Sync action and corrected the custom-sale cost
  option spacing.
